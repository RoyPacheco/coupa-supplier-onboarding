{
  "name": "coupa-cors-proxy",
  "version": "1.0.0",
  "description": "Local CORS proxy for Coupa API — Alo / Bella Canvas supplier onboarding",
  "main": "server.js",
  "scripts": {
    "start": "node proxy-server.js",
    "dev":   "nodemon proxy-server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "http-proxy-middleware": "^3.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
